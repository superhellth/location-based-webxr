# Captured Overpass payloads. The capture scripts write these minified on
# purpose — indentation is ~25 % of an OSM geometry payload's bytes — and
# prettier was silently undoing it on every `format` run, because this package
# was the only one in the workspace with no `.prettierignore` of its own and
# prettier resolves this file from its cwd without ever walking up to the
# workspace root. `sites/cologne-cathedral.non-areal.json` was written at
# 24.9 MB on one line and committed at 41.4 MB across 1 128 493 lines.
#
# See GpsPlusSlamJs_Docs/docs/2026-08-04-0709-pr-249-diff-weight-audit.md, and
# src/testdata/fixture-formatting.test.ts, which asserts the outcome.
src/testdata/**/*.json

# Generated by scripts/test-timing/ and rewritten by every gate run. Ignored in
# all nine sibling packages for the same reason; the osm format stage does not
# reach docs/ today, so this entry is parity rather than protection.
docs/test-timings.md
